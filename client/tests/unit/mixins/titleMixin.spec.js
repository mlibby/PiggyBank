import { shallowMount } from "@vue/test-utils";
import titleMixin from "@/mixins/titleMixin.js";

describe("titleMixin.js", () => {
    it("does this", () => {
        expect(true).toBe(true);
    });
});
